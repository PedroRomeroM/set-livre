import { IdentityRouteLoading } from "@/domains/identity/components/route-state";

export default function PasswordRecoveryLoading() {
  return <IdentityRouteLoading label="Preparando a recuperação de senha…" />;
}
