import { IdentityRouteLoading } from "@/domains/identity/components/route-state";

export default function RegistrationLoading() {
  return <IdentityRouteLoading label="Carregando cadastro e documentos legais vigentes…" />;
}
