import { FoundationStatus } from "@set-livre/ui";

export default function FoundationPage() {
  return (
    <FoundationStatus
      application="Plataforma pública"
      description="A fundação de aplicações, contratos, qualidade e segurança está ativa. A experiência pública será construída na FEAT-001."
      title="Set Livre"
    />
  );
}
