# Registros de mudança

Todo PR que altera código, infraestrutura, schema, CI ou comportamento cria:

`docs/changes/YYYY-MM-DD-<slug>.md`

Use o template em `docs/templates/change.md`.

O registro não substitui feature, ADR ou changelog; ele conecta a mudança concreta aos contratos afetados.
